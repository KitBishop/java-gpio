This directory contains the source .java files for a bare bones template for a Java program that accesses GPIO pins 
If you wish to keep your files in different directories, you will need to change the Makefile accordingly
